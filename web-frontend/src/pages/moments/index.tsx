import React from 'react';
import { withRouter } from 'react-router-dom';
import { Button } from 'antd-mobile';

function Moments() {
  return <Button type="warning">Moments</Button>
};

export default withRouter(Moments);
